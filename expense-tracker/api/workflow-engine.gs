// =============================================================================
// FULCRUM FORGE — Workflow Engine (removed)
// Account balances are computed at read time via _buildAccountNetMap in
// account-core.gs — they are not maintained incrementally. The workflow
// engine (executeWorkflow, reverseWorkflow, adjustAccountBalance) was dead
// code and has been deleted.
// =============================================================================
